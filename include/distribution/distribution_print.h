#ifndef _CPSFIT_DISTRIBUTION_PRINT_H
#define _CPSFIT_DISTRIBUTION_PRINT_H

//When a distribution or derived type is passed to an ostream, a printer object is obtained from a static manager, distributionPrint. The printer, which determines the format, and the policy that control what is printed can both be set by the user

#include<distribution/distribution_print/print_policy.h>
#include<distribution/distribution_print/printers.h>
#include<distribution/distribution_print/manager.h>

#endif
