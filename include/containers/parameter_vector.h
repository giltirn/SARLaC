#ifndef _SARLAC_PARAMETER_VECTOR_H_
#define _SARLAC_PARAMETER_VECTOR_H_

//A vector for storing fit parameters conforming to the usual spec.
// It is derived from NumericVector but with added operator* (which does not exist for NumericVector because its meaning would be unclear) - here it is the element-by-element product

#include<containers/parameter_vector/class.h>
#include<containers/parameter_vector/IO.h>

#endif
